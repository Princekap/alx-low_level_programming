#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char a = 'a';
	int b = 0;

	while
		(b < 10) {
			putchar(b + '0');
				b++;
		}
	while
		(a <= 'f') {
			putchar(a);
			a++;
		}
	putchar('\n');
	return (0);
}
