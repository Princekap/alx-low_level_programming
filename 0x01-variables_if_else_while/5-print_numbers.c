#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a = 0;

	while
		(a < 10) {
			printf("%d", a);
			a++;
		}
	putchar('\n');
	return (0);
}
